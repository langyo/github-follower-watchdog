{
  "name": "github-follower-watchdog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "preview": "npm --prefix site run preview",
    "typecheck": "npm --prefix site run typecheck",
    "clean": "npm --prefix site run clean"
  }
}
